pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAILONPROJECTREPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "CalmTaskVoiceAssistant"
include ":app"
